---
name: company-todo-audit
description: "公司 TODO 管理体系·闭环判定与回流 —— 在每次采集前，按闭环五要素判定上一周期每条 todo 是否真正闭环；未闭环项按原截止日算逾期天数，标题加 [逾期 N 天] 前缀回流进本周期派发列表；给 boss 出整体统计。Use when the todo-cycle-audit schedule fires (fire=prompt, cron '30 14 * * 1,3,5', 本周期 15:00 采集前半小时), or when explicitly asked to audit last cycle's closure status by hand. Must run before company-todo-sync in the same cycle — sync 依赖本技能算出的回流项。Companion skills: company-todo-sync (采集与派发), company-todo-review (评价回写)。动态一层:每轮判定结果落盘 .todo-eval 评测记录 + 逐人前后对比摘要(新开/承接/消失/回流/顺延)。动态防滑坡消费侧:本人最新两期填报文本相似度跳升 → 关注/干预 分级(私聊提醒 → mentor 报表需关注块 → boss/mentor 证据化名单)，与静态防复制同一相似度工具、两个出口。"
category: productivity
---

**离职/无法识别人员**:名单先交 `feishu_member_status_check` 分类(它只返回在职/解析失败名单和离职**人数**,不给离职姓名);已离职/冻结 → 不进对比,且**面向 mentor 的对比表/台账/报告完全不体现、不解释**;解析失败(unresolved,重名)→ 单列「解析失败,需人工」。不静默删除。

# 公司 TODO 管理体系 · 闭环判定与回流

> 判定口径读 `config/todo-sop.yaml`，用户可编辑，换公司只改此文件；本文保留引擎与通用纪律，参数值以该文件为准。

「交付完成」不等于「勾了框」。每次采集前半小时跑一次审计，判上一周期每条 todo 是否走完整圈；
没走完的带着逾期天数回流进本周期，绝不静默消失。

**必须早于 `company-todo-sync` 运行**：回流项要先算出来才能进入本次派发列表。两条 schedule 的
cron 已经错开（本技能 `30 14 * * 1,3,5`，sync `0 15 * * 1,3,5`），正常情况不需要手动排序。

## 用到的工具

- `feishu_bitable_search_records` — 拉上一周期全部 todo 行
- `feishu_api`（GET `/open-apis/task/v2/tasks/:task_guid`）— 查 `assignee_related[].completed_at`
  （见 `feishu-task` 技能表；main 上没有专用的 `feishu_task_get` 工具，任务域已迁移为端点表）
- `wiki_read` — 核对评价是否已回写进对应快照页
- `feishu_bitable_update_records` — 把回流项写进新周期（复制一行、改标题前缀、原截止日保留）
- `feishu_message_send_card` — 给 boss 推整体统计
- `feishu_text_similarity` — 判「本人最新两期」填报文本相似度（动态防滑坡消费，与静态防复制同一工具两个出口）

## 闭环五要素

五项齐备才置为「已闭环」，缺任一项都停在「未闭环」：

| # | 要素 | 从哪验证 |
|---|---|---|
| 1 | 有明确验收人 | 台账 `mentor` 字段非空 |
| 2 | 有自设截止时间且已到期或提前完成 | 台账截止日期 + 实际完成时间 |
| 3 | 负责人已勾选并提交成果物 | `feishu_api` 读任务详情，该负责人在 `assignee_related[]` 里的 `completed_at` 非空 |
| 4 | mentor 已给出评价与打分 | 台账 `mentor打分` 与 `mentor评语` 字段非空 |
| 5 | 评价已回写进本人 wiki 该条 todo 之后 | `wiki_read` 该快照页命中评语文本 |

判定第 3 项时**不要**用 `GET /tasks?type=my_tasks` 列出「机器人自己的任务」——那答的是「发请求身份
自己的任务」，不是某个员工的。要查某人某条任务做完没，直接读该任务详情，看
`assignee_related[]` 里那个人自己的 `completed_at`。

**判定结果写进台账**：五要素不是人工填的文本，而是本技能自动判定的结果——逐条判完后把**满足的
要素**勾进该行台账的「闭环五要素」多选字段（选项名与上表要素名一致），五项勾齐才允许把状态置
「已闭环」；缺任一项状态停在「进行中」/「未闭环逾期」。第 5 项只做**验证**：评价回写 wiki 的
动作由 `company-todo-review` 在 mentor 提交评价时立即执行，本技能用 `wiki_read` 核对快照页该
todo 之后确实有评语文本即可。

**删除线 = 上级验收通过的硬证据**(SOP v1.1:验收方式 = 上级用删除线划掉已完成且达标的 TODO)。删除线样式普通读表会拍平丢失,判验收状态时用确定性工具 `feishu_sheet_strike_read(board_link, person_name, cycle_date)` 读该人该期单元格的删除线:返回每段文本的 strike 状态。有删除线的段 = 已验收;无删除线 = 未验收(或验收未达标,守住「验收一定要守住标准」)。

## SOP v1.1 删除线验收衔接

真知TODO list SOP v1.1 把 TODO 定为小方案，验收方式暂定为：上级在 todo 库用**删除线**删除
已经完成和达到标准的 TODO，且验收一定要守住标准。与本文五要素的衔接：

- 删除线是**验收动作在 todo 库的呈现**（mentor 划掉已达标条目），不是台账里的状态字段；
- 它可作为**验收人已确认的看板侧佐证**（E2 级信号：写「已完成」的条目若已在库里被 mentor
  划删除线，其「已勾选并提交成果物/验收通过」的置信度上升），但**不豁免**第 3-5 项硬要件
  —— mentor 的打分评语与 wiki 回写仍按原通道判定，聊天里或看板上说过「验收了」都不顶用；
- 判定「已完成」仍以 E1（任务 `completed_at`）与 E2 硬证据为准，删除线与 E1 冲突时以 E1 为准并明写冲突。


## 第 0 步：评价回写扫描（表内直接打分的兜底通道）

mentor 有两条打分通道：评价卡（实时回写 wiki）与**台账表内直接填「mentor打分」「mentor评语」**
（更顺手，但没有事件通知 agent）。本步负责兜底，在五要素判定**之前**执行：

1. 拉上一周期全部行，找出「`mentor打分` 或 `mentor评语` 非空」但「闭环五要素里没有『评价已回写wiki』」
   的行。
2. 对每一行：`wiki_read` 该负责人本周期快照页，在对应 todo 条目之后插入评价（打分 + 评语 + 时间），
   其余内容原样保留，`wiki_write` 整页回写；然后勾上「评价已回写wiki」要素。
3. 扫描后**再**进入五要素判定——表内打分从此和评价卡同权，不存在「表里打了分但 wiki 漏回写」的死角。

## 回流规则

1. 遍历上一周期所有台账里状态非「已闭环」「请假顺延」的行，按原截止日算出逾期天数
   （今天 − 原截止日）。
2. 回流项**进入新周期的派发列表**（供 `company-todo-sync` 本轮一并派发），标题前缀
   `[逾期 N 天]`，原截止日保留在正文里，**不许静默改截止日**。
3. 连续两个周期未闭环的项，在该 mentor 的报表里单独列一块「持续逾期」，并同步给该 mentor 的上级
   （从其 wiki 页「上级：[[...]]」区块解析）。
4. 请假顺延项（`company-todo-sync` 第 2 步标记的）**不计入逾期天数**，但仍然回流——顺延不等于取消。
5. **回流不删除历史**：上一周期的快照页里该条仍然是「未闭环」原样保留，新周期页里是**新的一条**，
   两条通过标题（`[逾期 N 天] <原标题>`）互相对应，不做原地覆盖。

## 动态一层 · 评测落盘（.todo-eval）

CEO 口径：MVP 起即记录 check 结果数据，先搭评测体系再搭数据体系。动态一层的判定结果（每期谁闭环了、
谁回流了、谁顺延了）是这套数据里最核心的一块——落成结构化评测记录，形成个人跨周期连续性序列，
供前后对比、准确率评估与后续训练复用。

本技能每跑完一轮五要素判定，对**上一周期台账的每一行**写一条评测记录（落盘失败要明说，不静默丢结论）：

| 字段 | 含义 |
|---|---|
| date | 判定日（ISO） |
| cycle | 被审计的上一周期日期 |
| person | 负责人（open_id） |
| item | 标题 |
| item_type | 层级：大目标 / 小目标 / todo |
| verdict | 已闭环 / 未闭环逾期 / 回流[逾期 N 天] / 请假顺延 / 持续逾期 |
| missing_elements | 闭环五要素的缺项名（未闭环时才写） |
| evidence_level | 依据源：台账 / 任务 completed_at / wiki 回写 |
| evidence_refs | 台账 record_id、task_guid、wiki 快照页 |
| rules_hit | 命中的判定 / 回流规则 |

落点两处，同一条写两份：

1. workspace 评测数据目录 `.todo-eval/YYYY-MM-DD.json`：当日文件，**同 (cycle, person, item) 幂等覆盖**，
   重跑不重复积累；先读回当日已有记录再合并写回，不整文件覆盖丢历史。
2. `feishu_bitable_create_records` 同步飞书「评测记录」表（无则按与 JSON 一致的字段名新建；表结构沿用
   台账基建设计，人员列恒为人员类型）。

**用途**：跨周期拉同一人的记录即得连续性序列（闭环率、回流次数、持续逾期段、顺延段），是「动态一层
前后对比」与后续「动态二层成长评价」的数据底座。

## 前后对比摘要（上期 vs 本期，逐人）

在闭环判定之外，给**每个负责人**一份跨周期连续性摘要。**数据源：团队 TODO 看板表现场读**——
当期列 + 紧邻上一期列 + 人名列 + 上级列（mentor 分组按看板「上级」列，每次现场读、不写死；
本表所有姓名剥 `@` 提及前缀）。**不用台账、不用 wiki 快照**——台账是 sync 建行时的旧快照，
看板改过 mentor 之后台账不跟着变，按台账分组会把对比卡发到旧 mentor 手里（事故实况）。
台账类消费者（mentor 报表、评价卡补发）走本文「推送前置：先对账」节，与本节的看板路线分开。
对每人数六项：

| 项 | 口径 |
|---|---|
| 新开 | 本期列出现、上期列无同名标题 |
| 承接 | **仅限标题主体相同**:两期标题只在日期 / 序号 / 括号微调措辞上不同才算承接(例:「三个SOP视图打磨(9.18)」→「三个SOP视图打磨(持续)」)。**出现新交付物、新动作对象、验收标准改变的一律不算承接**(例:「测试集重设计」→「数据集扩展状态划定」算新开) |
| 消失 | 上期有、本期无(或标题已变) → 归 `todo-completion-standard` 的「推断已完成／待确认」，**不在这里判未闭环，更不判失实** |
| 已闭环 | 本期五要素齐 |
| 回流 | 上期未闭环回流进本期，含 [逾期 N 天] |
| 请假顺延 | 整周期 / 部分周期请假（不计入逾期天数） |

**判定顺序——标题完全相同的才是承接;删除线是验收完成的唯一硬证据(2026-09-21 定稿):**
逐人比对时:

1. **行号/列号先实读对齐**:人名列逐行实读拿到「姓名→行号」映射(剥 `@`);上期列/本期列按**表头日期列实读**确认(列名=日期文字,不猜字母),然后按行号读该成员两期单元格——**行号错一位、列猜错一个,删除线结论全部失真**(事故实况:按错行读删除线报「全划」,实际该成员格子没划线);
2. **承接的定义——仅限标题完全相同**:两期 TODO 标题**逐字相同**,只允许日期变化:日期改期(如(9.16)→(9.18))、或日期换成「持续」(如(9.18)→(持续))——「持续」是日期的延续写法,**视为日期变化,算承接**。**其余任何字面差异(哪怕主体词相同,如「测评整理」→「测评(统一模板)」、「优化方案撰写」→「模板优化」)一律不算承接**——「有一点字相同就是同名」是错判,会造成"同名再现"这类自造概念;
3. **上期条目**:查删除线 `feishu_sheet_strike_read(board_link, 姓名, 上期日期)`:划线 → 「已验收完成」;未划线 → 本期有完全相同标题 → 承接(该条延续);否则 → 「疑似完成(待确认)」(完成的不一定划线,保留不确定语气);
4. **本期条目**:与上期完全相同标题 → 承接;否则 → 新开。禁止「条目数对得上就整体判承接/整体判新开」的粗判;
5. **卡片 status 只报条目级结论,零项不报**:「已验收完成 N 项 / 承接 N 项 / 疑似完成 N 项 / 新开 N 项」四项中**只列非零类别**(0 项不写,如「疑似完成 3 项;新开 3 项」)。**不得把 run 级数字(如"删除线 1/3 run")、划线原始值、"同名再现/回流"等内部判据写进卡片**——那些是工具内部数据,卡片读者只关心"几条验收了、几条在延续、几条是新的";
6. **16:00 对比卡的 align_notes 一律传空**:对齐存疑/宽判说明是内部复核材料,不进卡片——卡片只呈现判定结果,读者有疑问再单独问;
7. **说明(notes)精简**:只写期次(上期=X 列、本期=Y 列)、判定口径一句话(✅=上期已划删除线,已验收完成;🔄承接=上一期的延续到了这一期;❓疑似完成=上一期没有删除线但这一期消失了)、请假免填/缺写等事实性补充;不写解释性条款(如「其余任何字面差异一律不算承接」这类规则复述)。

两份产出：

- **mentor 视图**（私聊，只含自己名下成员）：逐人六项 + 一句话连续性结论；连续两个周期未闭环的项单列
  「持续逾期」并同步该 mentor 上级（见回流规则第 3 条）。
- **boss 视图**：全公司汇总，每个 mentor 团队加「较上期」趋势列（闭环数、回流数、顺延数）。

### 推送前置：先对账人与 mentor 的关系（`feishu_todo_ledger_reconcile`）

**适用范围：按台账行分组的消费者（mentor 报表、评价卡补发等）。16:00 结构化对比卡
（`todo-ledger-push` 任务）不读台账、按看板 mentor 列实时分组（见上节数据源），不需要对账。**

**这些台账消费者的输出是按 mentor 分组的，而分组用的是台账行里的 `mentor` 字段 —— 那是 `company-todo-sync`
建行那一刻从看板抄下来的快照，之后没有任何流程会更新它。** 所以只要有人在建行之后改了看板的
mentor 列（把某人换到另一个 mentor 名下），这里就会继续按**改前的关系**分组出卡：人发到旧 mentor
手里、新 mentor 名下少一个人，而且发送本身会全绿（卡片内容是错的，投递是成功的）。

因此**凡要产出/推送逐 mentor 的分组（前后对比卡、mentor 报表、评价卡补发），先跑一次对账**：

```
feishu_todo_ledger_reconcile(
  board_link=<看板链接>,
  cycle_date=<本周期日期>,
  folder_token=<台账 base 所在文件夹>,   # 或改传 ledgers_json 逐个 base 的 app_token
  apply_fixes=false,                     # 先只看差在哪
  user_key=<发送者 open_id>,
)
```

按返回处理，三条都不许跳：

1. `relation_aligned: true` → 才可以说「按当前关系分组」，继续推送。
2. `mentor_changed` 非空 → 台账仍写着改前的 mentor。带 `apply_fixes=true` 重跑修正字段
   （只在名字唯一解析到一个通讯录条目时才写；解析不出的进 `needs_manual`，**交人工，不要自己
   挑一个同名的人写进去**）。
3. `needs_move` 非空 → **修正字段之后才会出现的那一半**：行还躺在旧 mentor 的
   `TODO 台账-<旧 mentor>` base 里。按 base 枚举的消费者仍会按旧关系读到它，所以此时
   **不得**声称已对齐——把行搬进新 mentor 的本周期表（`company-todo-sync` 的建行口径），
   或先如实报告「这两处还没对齐」。改字段与搬行是两件事。

`person_not_on_board` / `row_missing_mentor` / `board_missing_mentor` / `row_missing_owner` 四类
一律交人工补数据。其中**人不在看板上**的名字在转述给 mentor 之前，必须先过
`feishu_member_status_check` 分类（离职 / 冻结人员面向 mentor 的输出完全不体现，见本文件开头的
离职口径）——对账结果的这几桶**不是**可以直接贴给 mentor 的成品。

**纪律**：消失 ≠ 未闭环 ≠ 失实；请假顺延不计逾期；承接只对任务做去重、不对填报文本硬匹配（当前多数人
todo 还没写好父子关联，父项留空是常态，不强挂）。**16:00 对比卡比对看板两期列**（实时关系）；
台账类消费者比对台账与 wiki 快照两处权威源；一律不接受口头 / 聊天记录里的「说已经做完了」。

## 动态防滑坡 · 消费侧（本人最新两期相似度跳升 → 关注 / 干预）

CEO 对形式主义的第二担心是「半衰期滑坡」：强调时写得好、过几天开始滑坡（整段复制自己上期的填报，
或只改几个字敷衍）。静态出口（[`todo-truthfulness-check`] 的「防复制」节）抓**同周期跨人**复制，本文档的
消费侧出口抓**本人跨周期自复制**——同一把尺（`feishu_text_similarity`）两个出口，静态抓「应付」、
动态抓「滑坡」，只产证据与关注、不做机器定性。

**周期指代与本文档其余部分一致**：「前后对比摘要」里的本期 = 最新已建台账表对应周期、上期 = 其前一周期
历史表；本节沿用同一指代（本人**最新两期** = 摘要的本期 + 上期）。

**消费什么**：每跑完一轮判定，对每个负责人算一条「本人最新两期」填报文本相似度，随 `.todo-eval` 当日文件
**逐期追加**为 person 级记录（`cycle` 与同批 item 级记录一致、`item` 留空、`item_type=similarity`、`verdict` 写
`无需关注` / `关注·疑似自复制` / `关注·连续自复制` / `干预·防滑坡` 之一、`rules_hit` 写命中的跳升口径、
`evidence_refs` 写两期原文出处），键仍幂等（同 (cycle, person, item) 覆盖），重跑不重复积累。
**person 级相似度记录只落本地 `.todo-eval` JSON，不同步飞书「评测记录」表**（该表按 item 级设计）；
落盘失败要明说，不静默丢结论。

**相似度怎么算**：

- 文本取该负责人**本人**两期的 TODO 填报原文（台账行标题 / wiki 快照页的 TODO 段），**粒度只到 TODO 层**：
  大目标 / 小目标跨期稳定延续是常态，不参与比对（与 [`todo-truthfulness-check`] 防复制粒度一致）；
  跨周期**承接项**（标题逐字相同、仅日期可改期或换「持续」，复用原 task_guid，见「前后对比摘要」）是合法延续，
  先按承接口径剔除再比；剔除后剩余文本不足（如只剩 0-1 条）→ 记「无法判定」，不顺势关注。
- `feishu_text_similarity(text_a=本人上期 TODO 文本, text_b=本人本期 TODO 文本)`，默认 `threshold` 0.85
  （评审可调），命中读 `matched_fragment`（重复片段即证据）。

**跳升口径（判定规则，先软后硬、只列证据不贴标签）**：

每条 person 级相似度记录判定时，与该人**上一条**记录（上一对）比：

1. **无上一对记录**（首次进体系）→ 只落库作**基线**，不判、不提醒——没有历史就无从判断「跳升」。
2. **跳升**（上一对 < 阈值、本期对 ≥ 阈值 = 从正常水平突然跨过阈值，开始复制自己）→ 记
   「关注 · 疑似自复制」，**只私聊提醒本人**（带两期原文出处与 `matched_fragment`，请本人核对是否漏更新）。
3. **连续**（上一对 ≥ 阈值、本期对 ≥ 阈值）→ 升「关注 · 连续自复制」：进入该 mentor 报表
   「需关注 · 防滑坡」**单独块**（mentor 视图私聊，只含自己名下成员）。
4. **持续**（该人记录里最近三对都 ≥ 阈值）→ 升「干预 · 防滑坡」：同步 boss / mentor 的证据化**关注名单**
   （正负面清单雏形——CEO 口径待确认，先软后硬：只列 person + 相似度序列 + 命中片段 + 提醒史，
   不写「滑坡/敷衍」结论词）。
5. 本期对 < 阈值 → 记「无需关注」（回落正常，序列清零重计）。

**纪律（与「防误判纪律」同源）**：

- **数据不足不判**：上期无填报 / 无历史记录（本人新入职、首次进周期）→ 记「无基线 · 待积累」，不判；
  `.todo-eval` 里无该人历史记录 ≠ 滑坡证据，读表 / 读快照失败照报查询失败，不顺势关注。
- **关注 ≠ 违规 ≠ 失实**：相似度命中只进关注名单与提醒，**不进任何违规 / 失实判定、不改五要素闭环结论**
  （红线定性留人工）；报告逐条带 档位（关注级）+ 证据（相似度值 + 片段 + 出处）。
- 与静态防复制的边界：跨人复制归 [`todo-truthfulness-check`]，本文只消费**本人**跨期序列；两出口互不替代。

## E2 证据摄入（取证强化）

audit 的判定依据不只有台账、任务与 wiki。对以下两类条目**主动走 E2 通道补证**，减少「推断已完成／待确认」
占比、提升 check 准确率：

- 五要素判不齐、落到「推断已完成／待确认」的条目；
- **E1（任务 `completed_at`）取不到**的条目（没进任务系统、机器人不是任务成员、缺授权）。

E2 通道 = `feishu_message_search`（**user-token-only，必须带 `user_key`**）/ `feishu_thread_read`：检索该负责人
在群 / 文档评论里的**验收人明确说通过、交付确认、下游明确接手**等第三方确认；命中即进 audit 依据，注明来源
（消息 / 话题 id、命中时间）。纪律：

- E2 只给「推断 / 缺 E1」的条目**补第三方确认证据**，**不替代闭环五要素要件**——勾选交付（第 3 项）、
  mentor 打分评语（第 4 项）、wiki 回写（第 5 项）仍是置「已闭环」的硬要件，聊天里说过不顶用；
- 检索失败 / 无权限 → 照报 `unavailable`，取证对称性不变（见 `todo-completion-standard`）；
- E2 硬证据与自填 / 推断冲突时以硬证据为准，把冲突明写出来。

## 给 boss 的整体统计

`feishu_chart` 或直接文本卡列出：本周期大目标/小目标/todo 各多少条、已闭环多少、逾期多少、
请假顺延多少、各 mentor 下属平均分。整体统计与「前后对比摘要」的 boss 视图同源，一次查询合并出卡，
不重复读台账。boss 只读，不写 todo、不被派发——统计卡是私聊推送，不进群。

## 边界

- 五要素任一缺失都不能置「已闭环」，即便负责人已经勾选卡片——「勾了框」只满足第 3 项。
- 逾期天数按**原截止日**算，不是按上次审计日算；不许因为回流就悄悄顺延一个新截止日。
- 判定用的数据只信台账 + 飞书任务详情 + wiki 快照页三处权威源，不接受口头/聊天记录里的「说已经做完了」；E2 检索（`feishu_message_search` / `feishu_thread_read`）只用于给「推断 / 缺 E1」条目补第三方确认证据，不替代五要素要件。
